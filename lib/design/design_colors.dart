import 'package:flutter/material.dart';

///
/// A utility class to access colors used in the design system
///
class UDesignColors {
  static const Color primary = Color(0xFF080808);
  static const Color secondary = Color(0xff909090);
  static const Color border = Color(0xFFEEEEEE);
}
